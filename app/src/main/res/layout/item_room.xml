<?xml version="1.0" encoding="utf-8"?>
<!--
ОРИГІНАЛЬНИЙ LAYOUT:
- Тільки TextView з назвою

ПОКРАЩЕНИЙ LAYOUT:
✅ Material Design Card
✅ Індикатор статусу (кольорова лінія)
✅ Назва + статус + опис
✅ Ripple effect при кліку
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardElevation="2dp"
    app:cardCornerRadius="8dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Індикатор статусу (кольорова лінія зліва) -->
        <View
            android:id="@+id/status_indicator"
            android:layout_width="4dp"
            android:layout_height="0dp"
            android:background="#4CAF50"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"/>

        <!-- Назва аудиторії (ОРИГІНАЛЬНЕ ПОЛЕ) -->
        <TextView
            android:id="@+id/room_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@android:color/black"
            tools:text="Аудиторія 305"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@id/status_indicator"
            app:layout_constraintEnd_toStartOf="@id/room_status"/>

        <!-- Статус (ДОДАНО) -->
        <TextView
            android:id="@+id/room_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textStyle="bold"
            tools:text="Вільна"
            tools:textColor="#4CAF50"
            app:layout_constraintTop_toTopOf="@id/room_name"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Опис (поверх, вмістимість) (ДОДАНО) -->
        <TextView
            android:id="@+id/room_description"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginTop="4dp"
            android:textSize="14sp"
            android:textColor="@android:color/darker_gray"
            tools:text="3 поверх • 30 місць"
            app:layout_constraintTop_toBottomOf="@id/room_name"
            app:layout_constraintStart_toEndOf="@id/status_indicator"
            app:layout_constraintEnd_toEndOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>